# 项目迭代记录

## 记录规则
- 每次动手前记录本轮目标、背景、影响范围
- 每次改完后记录实际修改文件、验证方式、结果、遗留问题
- 当线上现象与本地不一致时，单独记录部署、缓存、数据库状态

## 当前项目基线
- 后台支持新增文章、待审核转发布、删除文章、编辑文章
- AI 提取支持 Kimi / DeepSeek
- article_details 已接入数据库持久化
- 删除链路已引入数据库级联删除，防止孤儿记录

## 最近一轮关键迭代
### 目标
- 修复 DeepSeek 解读格式与 Kimi 不一致
- 在新增文章弹窗显示“解读”输入框
- 固化用户长期协作原则与记录机制

### 主要改动
- 新增文章弹窗增加解读展示与编辑框
- 新增文章提取结果进入弹窗前先做解读格式清洗
- 摘要/解读生成服务补充更严格的格式约束
- article_details 读写增加解读格式归一化
- 新建项目框架.md、todolist.md、项目迭代记录.md
- 更新 .trae/rules/project_rules.md

### 影响文件
- src/components/admin/AdminAddArticleDialog.tsx
- src/components/AdminDashboard.tsx
- src/hooks/useAdminArticleManagement.ts
- src/services/aiSummaryService.ts
- src/services/kimiArticleService.ts
- src/services/articleDetailService.ts
- .trae/rules/project_rules.md
- 项目框架.md
- todolist.md
- 项目迭代记录.md

### 验证结果
- 已通过 build / eslint / tsc
- 已做后台真人模拟测试，确认新增文章弹窗中存在“解读”输入框
- 已确认最新修复推送到远端 main

### 当前提醒
- 后续每轮开始前先更新本文件
- 后续每轮结束后补充“实际结果、风险、下一步”
- 如涉及部署问题，要补充远端提交号与线上验证结论

## 最新一轮优化评估
### 结论
- 当前主链路已经可用，后续优化应优先聚焦“减少重复逻辑、降低回归风险、增强部署可见性、补足历史数据治理”。

### 建议优先级
- 高优先级：统一解读格式清洗逻辑，避免同类规则分散在多个文件中重复维护。
- 高优先级：补一轮历史 article_details 解读文本批量清洗，避免旧数据继续展示旧格式。
- 中优先级：把部署状态、最新提交号、Actions 结果直接展示到后台，减少“到底有没有部署成功”的反复确认。
- 中优先级：给新增文章 / 删除文章 / AI 提取链路补自动化回归测试。
- 中优先级：给关键后台操作加更清晰的日志与状态反馈。

## 当前进行中的第一步
### 目标
- 先完成“统一解读格式清洗逻辑”这一项优化。
- 以不影响现有全部功能为第一前提。

### 预期方式
- 抽出共享函数，避免 aiSummaryService、kimiArticleService、articleDetailService、useAdminArticleManagement 各自维护同类正则。
- 保持现有输入输出行为一致，只减少重复逻辑与后续回归风险。

### 实际改动
- 在 src/lib/utils.ts 新增共享函数 normalizeAnalysisText，统一处理标题规范化、提示语剔除、概念解释前缀剔除、空白折叠。
- src/services/aiSummaryService.ts 改为直接复用共享函数，保持摘要/解读生成链路输出一致。
- src/services/kimiArticleService.ts 改为在提取结果解析后统一调用共享函数，保证新增文章链路与详情重生成链路一致。
- src/services/articleDetailService.ts 改为在云端读取、本地缓存读取、保存、同步时统一调用共享函数，并修正了本地缓存读取处的遗漏引用。
- src/hooks/useAdminArticleManagement.ts 改为统一复用共享函数，保证新增文章弹窗预览与最终入库文本一致。

### 影响文件
- src/lib/utils.ts
- src/services/aiSummaryService.ts
- src/services/kimiArticleService.ts
- src/services/articleDetailService.ts
- src/hooks/useAdminArticleManagement.ts
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：node .\\node_modules\\vite\\bin\\vite.js build --emptyOutDir
- 已通过 lint：node .\\node_modules\\eslint\\bin\\eslint.js .
- 已通过类型检查：node .\\node_modules\\typescript\\bin\\tsc --noEmit
- 已做真人模拟测试：后台文章管理页正常打开，新增文章弹窗正常显示“解读”输入框，前台详情页正常展示摘要与解读
- 管理后台控制台出现的 404 为 /favicon.ico，请求与本轮改动无关

### 下一步
- 推送本轮第一步优化到远端 main，触发既有自动部署流程

## 当前进行中的第二步
### 目标
- 处理历史 article_details 中仍可能保留旧格式的解读文本，避免旧数据继续展示旧写法。
- 评估并按最小范围处理 /favicon.ico 404，前提是不影响当前全部功能。

### 预期方式
- 保持现有前端运行时兜底清洗逻辑不变，再补一个可单独执行的历史数据批量清洗脚本。
- favicon 优先采用已有静态资源做最小接入，避免新增复杂依赖或改动主链路。

### 实际改动
- index.html 新增站点图标声明，直接复用 public/share-cover.svg，避免浏览器再默认请求根路径 /favicon.ico。
- 新增 scripts/normalize_article_details_analysis.py，支持按统一规则批量扫描和清洗 article_details.analysis 历史数据。
- 继续保持现有前端运行时的 normalizeAnalysisText 兜底逻辑，确保脚本执行前后展示结果一致。

### 实际数据处理
- 通过已登录管理员会话对 article_details 全量排查，共扫描 1138 条记录。
- 排查结果显示仅 1 条历史记录仍需清洗，记录 ID 为 P2024-0257。
- 已实际完成该记录的 analysis 字段清洗，并复查确认剩余需清洗数量为 0。

### 影响文件
- index.html
- scripts/normalize_article_details_analysis.py
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 Python 语法校验：python -m py_compile .\\scripts\\normalize_article_details_analysis.py
- 已通过 build：node .\\node_modules\\vite\\bin\\vite.js build --emptyOutDir
- 已通过 lint：node .\\node_modules\\eslint\\bin\\eslint.js .
- 已通过类型检查：node .\\node_modules\\typescript\\bin\\tsc --noEmit
- 已做真人模拟测试：首页正常加载，详情页摘要与解读正常展示，favicon 请求改为 /speech-web/share-cover.svg 且返回 200
- 当前仍存在的 404 来自 analytics 对 Supabase 表名 new_table 的回退探测，不属于 favicon 问题

### 下一步
- 推送第二步优化到远端 main，触发自动部署并复查线上结果

## 当前进行中的第三步
### 目标
- 消除 analytics 表名探测时产生的 404，避免无意义报错干扰排查。
- 保证首页统计埋点、后台访问统计、现有数据写入与读取能力全部保持正常。

### 预期方式
- 不改动统计表结构，不改动埋点业务逻辑。
- 仅优化表名解析顺序与缓存策略，让系统优先命中当前真实存在的统计表。

### 实际改动
- src/services/analytics.ts 调整表名候选顺序，优先使用 New table，并补上 localStorage 缓存读取与保存逻辑。
- src/services/supabaseAnalytics.ts 调整同样的表名优先级与默认回退策略，保持后台统计链路一致。
- 保持原有多候选表名兼容能力，避免对其他环境的容错能力下降。

### 影响文件
- src/services/analytics.ts
- src/services/supabaseAnalytics.ts
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：node .\\node_modules\\vite\\bin\\vite.js build --emptyOutDir
- 已通过 lint：node .\\node_modules\\eslint\\bin\\eslint.js .
- 已通过类型检查：node .\\node_modules\\typescript\\bin\\tsc --noEmit
- 已做真人模拟测试：首页正常加载且 analytics 表探测请求为 200
- 已做真人模拟测试：后台访问统计页正常加载，统计数字与最近访问记录正常显示，相关表探测均为 200
- 已确认 favicon 请求继续返回 200，详情页展示未受影响

### 下一步
- 推送第三步优化到远端 main，触发自动部署并复查线上结果

## 当前进行中的第四步
### 目标
- 优化人民网文章的领域识别准确率，减少仅凭 AI 默认值落入 politics 的情况。
- 在不影响现有提取链路的前提下，优先参考人民网 URL / 栏目特征做兜底修正。

### 预期方式
- 不替换现有 AI 提取逻辑，只在提取结果领域缺失或默认为 politics 时再使用 URL 特征纠偏。
- 保持非人民网来源与已被 AI 正确识别为非政治领域的文章不受影响。

### 实际改动
- src/services/kimiArticleService.ts 新增人民网 URL 栏目特征识别逻辑，并集中在 refineExtractedArticle 中对提取结果做收口处理。
- 识别范围覆盖 people.com.cn 与 jhsjk.people.cn 的常见栏目特征，对 economy、party、diplomacy、defense、society、culture、ecology 提供兜底修正。
- src/hooks/useAdminArticleManagement.ts 补上手动粘贴提取成功后的 domain / domainName 回填，保证与 URL 提取链路一致。

### 影响文件
- src/services/kimiArticleService.ts
- src/hooks/useAdminArticleManagement.ts
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：node .\\node_modules\\vite\\bin\\vite.js build --emptyOutDir
- 已通过 lint：node .\\node_modules\\eslint\\bin\\eslint.js .
- 已通过类型检查：node .\\node_modules\\typescript\\bin\\tsc --noEmit
- 已确认该修正只在领域缺失或为 politics 时生效，不会覆盖 AI 已明确给出的非政治领域结果
- 已用代表性人民网 URL 样本校验 finance、cpc、world、military、health、culture、env 等栏目映射结果符合预期，politics 栏目保持不改写
- 已确认首页、详情页、后台访问统计相关改动不受本轮影响

### 下一步
- 推送第四步优化到远端 main，触发自动部署并继续观察人民网新提取文章的分类表现

## 当前进行中的第五步
### 目标
- 优化人民网文章类型识别，减少“发表文章 / 重要会议 / 考察调研”被默认归到重要讲话的情况。
- 保持现有 AI 提取主逻辑不变，只在明显可识别的人民网标题 / 栏目场景下做保守纠偏。

### 预期方式
- 不直接取代 AI 类型判断。
- 仅在提取结果类型缺失或仍为默认 speech 时，使用人民网 URL 和标题强特征做兜底修正。

### 实际改动
- src/services/kimiArticleService.ts 新增 CATEGORY_NAME_MAP 与 inferPeopleCategoryFromArticle。
- 文章类型兜底优先识别评论员文章 / 《求是》 / 理论栏目为 article，识别考察 / 调研 / 植树 / 慰问为 inspection，识别召开会议 / 会见 / 会谈 / 审议 / 开幕 / 闭幕为 meeting，识别贺信 / 贺电 / 致电 / 回信 / 复信 / 指示 / 命令 / 致辞 / 讲话为 speech。
- refineExtractedArticle 改为同时收口 category / categoryName 与 domain / domainName，保证 URL 提取链路输出更稳定。

### 影响文件
- src/services/kimiArticleService.ts
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：node .\\node_modules\\vite\\bin\\vite.js build --emptyOutDir
- 已通过 lint：node .\\node_modules\\eslint\\bin\\eslint.js .
- 已通过类型检查：node .\\node_modules\\typescript\\bin\\tsc --noEmit
- 已用代表性人民网标题 + URL 样本模拟验证：评论员文章与《求是》文章归为 article，植树活动归为 inspection，政治局会议归为 meeting，致电祝贺归为 speech
- 已确认该修正仅在 category 缺失或仍为 speech 时介入，不会覆盖 AI 已明确识别出的 article / meeting / inspection
- 已确认首页、详情页、后台统计页未受影响

### 下一步
- 推送第五步优化到远端 main，触发自动部署并继续观察人民网新提取文章的类型表现

## 当前进行中的第六步
### 目标
- 提升人民网文章的领域类型识别精度，减少经济、外交、社会、生态等内容被默认归到 politics。
- 在不破坏现有提取链路的前提下，把领域纠偏从单纯 URL 栏目扩展到内容语义层面。

### 预期方式
- 保持 AI 提取主逻辑不变，只在领域缺失或仍为默认 politics 时，使用人民网来源特征做保守兜底。
- 优先相信明确的人民网栏目，其次再用标题、来源、摘要、正文关键词综合评分，避免只看 URL 导致误判。

### 实际改动
- src/services/kimiArticleService.ts 新增 DOMAIN_KEYWORDS、scoreDomainByText、inferDomainFromArticleContent。
- refineExtractedArticle 改为先尝试 inferPeopleDomainFromUrl，再回退到 inferDomainFromArticleContent，对默认 politics 做更细粒度纠偏。
- 移除了 theory.people.com.cn 被直接归到 party 的硬编码匹配，避免《求是》经济类文章继续误落党建。

### 影响文件
- src/services/kimiArticleService.ts
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：node .\\node_modules\\vite\\bin\\vite.js build --emptyOutDir
- 已通过 lint：node .\\node_modules\\eslint\\bin\\eslint.js .
- 已通过类型检查：node .\\node_modules\\typescript\\bin\\tsc --noEmit
- 已通过 diagnostics 检查，当前工作区无新增报错
- 已用代表性人民网标题 + URL + 摘要 / 正文样本模拟验证：海洋经济文章归为 economy，拉共体峰会贺信归为 diplomacy，植树活动归为 ecology，卫生健康相关座谈归为 society，政治局会议保持 politics
- 模拟验证过程中没有再出现“theory.people.com.cn 一律归党建”或“多数样本回落政治”的问题

### 用户可复测方式
- 在后台新增文章中分别粘贴人民网经济、外交、社会、生态、政治五类文章链接，提取后检查领域是否分别显示为“经济、外交、社会、生态、政治”
- 重点复测 theory.people.com.cn 的《求是》经济类文章，确认不再被误判成“党建”或“政治”
- 再复测一篇 politics.people.com.cn 的政治局会议文章，确认本轮增强没有把纯政治文章误改成其他领域

### 下一步
- 推送第六步优化到远端 main，触发现有自动部署并继续观察新提取文章的领域表现

## 当前进行中的第七步
### 目标
- 继续提升人民网领域识别精度，解决用户反馈的“领域类型还是容易都识别成政治”问题。
- 同步收紧摘要生成长度，避免摘要过长。
- 排查首页首次进入、详情返回首页时的卡顿和错误边界提示。
- 核查今天上午额外“搜索工作流”触发来源。

### 预期方式
- 在不替换现有 AI 提取主流程的前提下，为领域识别增加更强的标题/摘要强信号优先级，再结合扩展关键词评分保守纠偏。
- 不只依赖提示词约束摘要长度，同时在本地增加抽取式摘要和超长裁剪兜底。
- 前端首页优先使用本地缓存回填，并提前预加载详情页组件，减少首屏和往返跳转等待。
- 工作流排查以仓库 workflow 配置和 Actions 运行记录为准，不凭页面印象判断。

### 实际改动
- src/services/kimiArticleService.ts 扩展 economy、diplomacy、society、ecology 等领域关键词，并新增 inferDomainFromStrongSignals，让标题/摘要里出现明显主题词时优先落到具体领域。
- src/services/kimiArticleService.ts 同步增强提取提示词，明确要求不要因为文章位于 politics.people.com.cn 等时政频道就一律判成 politics，必须结合标题、摘要、正文主题判断领域。
- src/services/aiSummaryService.ts 将摘要要求从 150-200 字收紧为 80-120 字，并新增 buildExtractiveSummary、clampSummaryLength、normalizeGeneratedSummary，避免摘要失控变长。
- src/App.tsx 引入 getLocalArticlesSync 作为 SWR fallback，并在空闲时预加载 DetailPage。
- src/components/ContentList.tsx 在列表卡片 hover / focus 时预加载详情页，并为异常 category 加入兜底配置。
- src/components/DetailPage.tsx 对 categoryConfig 读取加入兜底，减少脏数据触发错误边界。
- src/services/articleServiceEnhanced.ts 在 fromDbFormat 中补齐 category、domain、categoryName、domainName、source、summary、url 等默认值，降低首页和详情页被脏数据击穿的概率。

### 影响文件
- src/services/kimiArticleService.ts
- src/services/aiSummaryService.ts
- src/App.tsx
- src/components/ContentList.tsx
- src/components/DetailPage.tsx
- src/services/articleServiceEnhanced.ts
- .github/workflows/fetch-articles.yml
- .github/workflows/ai-auto-search.yml
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：& npm.cmd run build
- 已通过 lint：& npm.cmd run lint
- 已通过类型检查：& npx.cmd tsc --noEmit -p tsconfig.app.json
- 已通过 diagnostics 检查，当前工作区无新增报错
- 已在本地页面模拟验证：首页可直接打开，点击文章进入详情页正常，浏览器返回首页后列表可正常恢复，未再出现“页面在加载时遇到了意外错误”
- 已复核工作流配置：fetch-articles.yml 当前仅保留 workflow_dispatch，schedule 已注释停用；今天上午自动搜索主要来自 ai-auto-search，未发现 fetch-articles 被额外定时触发

### 用户可复测方式
- 后台新增 5 篇人民网样例链接，分别覆盖经济、外交、社会、生态、政治，检查提取结果的领域是否更贴近文章主题而不是大面积落到政治。
- 任意打开一个详情页，点击“AI生成”重新生成摘要，确认摘要明显缩短，读完即可知道原文核心内容，且尽量沿用原文表述。
- 首次打开首页、点标题进入详情、再返回首页，观察是否仍有明显卡顿或“页面在加载时遇到了意外错误”提示。

### 下一步
- 继续处理第六步遗留的远端同步问题，确认本地领域识别增强真正进入 GitHub main 与部署环境

## 当前进行中的第八步
### 目标
- 将“AI 搜索优先模型”和“URL 新增文章识别优先模型”拆分配置，满足用户“搜索优先 Kimi、URL 识别优先 DeepSeek”的独立控制需求。

### 预期方式
- 保留原有搜索偏好存储，不影响搜索链路。
- 为新增文章提取链路单独增加一个偏好存储键，并在后台配置弹窗中拆成两套设置入口。

### 实际改动
- src/services/aiSearchService.ts 新增 preferred_article_extraction_api 存储键，以及 getPreferredExtractionApi、setPreferredExtractionApi。
- src/services/kimiArticleService.ts 改为读取 getPreferredExtractionApi，让 URL 提取/识别链路使用独立偏好，而不再复用搜索偏好。
- src/hooks/useAdminArticleManagement.ts 改为在后台新增文章流程中读取新增文章识别专用偏好，并据此决定优先走 Kimi 还是 DeepSeek。
- src/components/AdminDashboard.tsx 新增 preferredExtractionApi 状态与切换处理逻辑。
- src/components/admin/AdminApiConfigDialog.tsx 将原本单一的“搜索时优先使用”拆分为“搜索时优先使用”和“URL新增文章识别时优先使用”两组独立按钮。

### 影响文件
- src/services/aiSearchService.ts
- src/services/kimiArticleService.ts
- src/hooks/useAdminArticleManagement.ts
- src/components/AdminDashboard.tsx
- src/components/admin/AdminApiConfigDialog.tsx
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已通过 build：& npm.cmd run build
- 已通过 lint：& npm.cmd run lint
- 已通过类型检查：& npx.cmd tsc --noEmit -p tsconfig.app.json
- 已通过 diagnostics 检查，当前工作区无新增报错
- 已确认代码层面搜索链路继续读取 preferred_search_api，新增文章识别链路改为读取 preferred_article_extraction_api，两个设置已经解耦

### 用户可复测方式
- 打开后台“管理 API”，把“搜索时优先使用”设为 Kimi，把“URL新增文章识别时优先使用”设为 DeepSeek。
- 关闭对话框后重新打开，确认两个设置分别保留。
- 进入“新增文章”弹窗，确认顶部“已配置 …”标识跟随 URL 识别优先项变化；再执行 AI 搜索，确认搜索优先项不受新增文章识别设置影响。

### 下一步
- 继续处理第六步遗留的远端同步问题，并在需要时把这轮偏好拆分一并带入部署验证

## 当前进行中的第九步
### 目标
- 复核用户最新线上反馈，确认“首页卡顿”和“摘要仍不够短”到底是未部署还是现有修复力度不够。
- 在确认已部署的前提下，补更强的摘要压缩和首页列表摘要治理，直接解决用户仍能感知到的问题。

### 预期方式
- 先核对远端 main 与部署状态，避免误判为本地未上线。
- 把摘要长度收敛逻辑从多处分散实现收敛成统一方法，避免不同页面各自裁剪规则不一致。
- 让首页文章列表也统一使用短摘要，减少长文本渲染带来的首屏和返回首页卡顿。

### 实际改动
- 复核 GitHub 远端文件与部署记录，确认 aiSummaryService、App.tsx 等改动已在 main 上，且 Deploy static content to Pages 已成功完成。
- src/lib/utils.ts 新增 normalizeSummaryText，统一处理“去掉摘要标签、压缩空白、限制长度、最多两句、首句过长时直接截断”。
- src/services/aiSummaryService.ts 改为复用 normalizeSummaryText，并把摘要提示词进一步收紧为“最多两句、优先摘用原文关键句、不要补充原文没有的信息”。
- src/services/articleDetailService.ts 与 src/components/DetailPage.tsx 改为统一复用 normalizeSummaryText，避免详情页仍显示超长旧摘要。
- src/services/articleServiceEnhanced.ts 在数据库映射、本地缓存读取、本地缓存写入、静态兜底中统一压缩 summary，让首页列表直接使用短摘要。

### 影响文件
- src/lib/utils.ts
- src/services/aiSummaryService.ts
- src/services/articleDetailService.ts
- src/components/DetailPage.tsx
- src/services/articleServiceEnhanced.ts
- todolist.md
- 项目迭代记录.md
- 项目框架.md

### 验证结果
- 已确认远端 main 包含此前摘要与首页优化代码，不是“未推送导致线上无变化”
- 已通过 build：& npm.cmd run build
- 已通过 lint：& npm.cmd run lint
- 已通过类型检查：& npx.cmd tsc --noEmit -p tsconfig.app.json
- 已通过 diagnostics 检查，当前工作区无新增报错
- 已在本地页面复测：首页列表摘要已经压缩到更短文本；详情页摘要也被收敛到简洁长度，不再整段超长展示

### 用户可复测方式
- 强制刷新首页，观察列表卡片摘要是否明显缩短到 1-2 句。
- 任选一篇详情页，查看当前摘要是否明显短于之前。
- 点击“AI生成”重新生成摘要，确认输出仍为简洁摘要，且尽量沿用原文表述。

### 下一步
- 将第九步补强修复再次推送部署，并继续跟踪用户线上复测结果

## 当前进行中的第十步
### 目标
- 直接核实用户反馈的“首页卡顿、摘要仍偏长”是否因为第九步补强修复没有真正发布到远端 main。
- 在遵守长期规则的前提下，先同步三份根目录文件状态，再把遗漏的远端发布补齐。

### 预期方式
- 先复核 .trae/rules/project_rules.md、根目录三文件、本地 HEAD 与远端 main 的实际提交号，避免再凭本地 git 输出误判已部署。
- 若远端仍停留在旧提交，则直接补发布第九步相关文件，并再次验证远端文件内容与部署结果。

### 当前确认结果
- 已重新复核长期规则，确认本轮开始前应先同步根目录记录文件。
- 已核对三份根目录文件，确认本地记录已写到第九步补强修复。
- 已核对本地 main 当前 HEAD 为 95fb260，但 GitHub 远端 main 最新提交仍是 b5e9797，说明第九步补强修复尚未真正发布成功。
- 已进一步核对远端 src/lib/utils.ts 与 src/components/DetailPage.tsx，确认远端仍缺少 normalizeSummaryText 及其在详情页中的统一接入。

### 下一步
- 重新发布第九步补强修复与三份根目录文件。
- 发布后再次核对远端 main 文件内容与部署状态，再回写最终结果。

### 实际结果
- 已重新执行远端核对，确认此前通过 GitHub 内容接口已同步成功的文件包括 src/services/aiSummaryService.ts、src/services/articleDetailService.ts、src/services/articleServiceEnhanced.ts 与三份根目录文件。
- 已继续补发远端仍缺失的 src/lib/utils.ts 和 src/components/DetailPage.tsx，使远端 finally 包含 normalizeSummaryText 及详情页统一摘要压缩接入。
- 已再次核对远端文件内容，确认首页列表摘要压缩、详情摘要压缩、AI 摘要压缩三条链路现在都已进入远端 main 对应文件。
- 已重新执行 build：npm.cmd run build、lint：npm.cmd run lint、类型检查：npx.cmd tsc --noEmit -p tsconfig.app.json，并再次确认 diagnostics 为 0。

### 当前结论
- 用户此前线上看到“首页仍卡、摘要仍偏长”，主要原因不是根本没做修复，而是第九步补强修复当时没有完整进入远端；尤其 utils.ts 与 DetailPage.tsx 缺失会直接导致摘要压缩不完整。
- 当前远端代码已补齐第九步与第十步核对结果，线上后续部署应能够真正体现更强的摘要收敛与首页摘要治理效果。

### 下一步
- 等待线上页面刷新部署后，按“首页强刷 -> 详情 -> 返回首页 -> AI生成摘要”路径继续复测用户体感结果。

## 当前进行中的第十一步
### 目标
- 排查自动搜索工作流为什么漏掉今天《求是》杂志发表的总书记文章。
- 恢复并补强“人民日报/人民网、新华社/新华网、求是杂志、人民网总书记讲话”这几类来源的自动搜索覆盖，并在多来源之间去重。

### 预期方式
- 先检查 .github/workflows/ai-auto-search.yml 调用的 .github/scripts/ai_search.py，而不是只看前端 aiSearchService。
- 重点确认是否存在“只直抓人民网讲话库，没有直抓求是/新华/人民网总书记专栏”“AI 搜索提示词未包含求是”“仅按 URL 去重没有按标题去重”等问题。
- 若确认缺失，则在工作流脚本中补齐官方来源直抓与标题去重，再重新验证。

### 当前排查结论
- 当前定时工作流实际运行的是 .github/scripts/ai_search.py，不是前端的 aiSearchService。
- 该脚本当前只有人民网讲话数据库的直接抓取，没有对求是网、新华网、人民网总书记专栏做同等级直抓，这会导致《求是》杂志新文章只能依赖 Kimi 联网搜索或百度结果补漏，稳定性不够。
- 当前搜索查询词只显式包含“人民网 新华社”，没有把“求是/求是杂志”写进主查询。
- 当前脚本只按 URL 过滤数据库已有内容，没有像 discover_articles.py 那样按标题做跨来源去重，这与用户原来的搜索逻辑不完全一致。

### 下一步
- 直接修复 ai_search.py：补齐求是网、新华网、人民网总书记专栏直抓，增强搜索提示词，并加入标题去重逻辑。
- 修复后执行构建检查与脚本级针对性验证，再回写最终结果。

### 实际改动
- ai_search.py 已新增 URL 规范化，jhsjk.people.cn 同一篇文章的普通链接和 ?isindex=1 链接现在按同一篇处理，避免“文章库已有但待审核又进一条”。
- ai_search.py 已取消人民网讲话数据库“标题必须显式含习近平/总书记”的误筛条件，因此《树立和践行正确政绩观》这类标题不带人名但确属总书记原文的文章不再漏掉。
- ai_search.py 已把求是网抓取改为“抓候选链接后，再按正文页判定是否为原文”，要求正文页出现“作者：习近平”，并将标题改写为正文原始标题，过滤“《求是》杂志发表习近平总书记重要文章”这类发布消息页。
- ai_search.py 已把新华社/新华网、人民网总书记专栏等来源改为只保留总书记直接原始内容，过滤“总书记关切事”“和总书记面对面”“学思践悟”等评论、解读、综述类标题。

### 针对性测试结果
- 本地按 2026-03-31 重新抓取后，人民网讲话数据库已能命中 `https://jhsjk.people.cn/article/40692603`，标题为《树立和践行正确政绩观》。
- 本地按“正文页必须出现作者：习近平”的规则重新筛选后，求是网会保留原文页 `https://www.qstheory.cn/20260331/904c5f95a8424cd0a5fbfb3f58a67d56/c.html`，并过滤此前误收的发布消息页与评论解读页。
- 在不连接线上数据库的本地测试里，收紧后当天原始候选共 5 篇人民网原文；若按用户补充说明将其中已在文章库中的 4 篇视为已存在，则最终仅剩 1 篇新增候选：《树立和践行正确政绩观》。

### 当前结论
- 这轮问题的关键不是“搜索不到求是来源本身”，而是原有规则同时存在“人民网原文误筛掉”和“求是原文/求是发布消息没区分开”两类问题。
- 当前收紧后，搜索逻辑已经更贴近用户要求：只保留总书记直接原文，不收评论解读；同文跨来源去重；文章库已有内容不应再进入待审核。

### 下一步
- 等待用户确认当前测试结果符合预期后，再决定是否推送部署并继续联调线上数据库去重结果。

### 用户追加规则后的继续优化
- 已继续补充“来源优先级”规则：同一篇总书记原文如果同时出现在求是/新华社/人民日报与人民网，则优先保留求是/新华社/人民日报来源，人民网只在其他来源缺失时兜底。
- 已在 ai_search.py 中加入 source priority，当前排序为求是网 > 新华网 > AI/百度补漏 > 人民网总书记专栏 > 人民网讲话数据库。
- 已重新模拟 2026-03-31 的抓取结果：在不连接线上数据库的情况下，去重后原始候选共 5 篇，其中《树立和践行正确政绩观》现优先保留为求是网原文链接，而不是人民网讲话库链接。
- 已按用户补充说明把那 4 篇人民网原文视为文章库已存在后再次模拟过滤，结果最终仅剩 1 篇新增候选：`https://www.qstheory.cn/20260331/904c5f95a8424cd0a5fbfb3f58a67d56/c.html`。

### 当前结论更新
- 现在的本地结果已经与用户预期一致：若线上文章库中确实已有那 4 篇人民网原文，则今天自动搜索最终应只新增 1 篇求是原文《树立和践行正确政绩观》。
- 下一步可直接推送部署，再观察线上工作流实际写入结果是否与本地模拟一致。

### 2026-03-31 晚间线上复测反馈
- 用户在线上看到的自动搜索记录为：执行成功、爬取 24 条、搜索 0 条、新增 0 条，其中来源统计为 Kimi 0、百度 0，说明线上工作流虽然已运行，但最终没有写入新增待审核。
- 这与本地“最终应剩 1 篇求是原文候选”的模拟结果不一致，因此明天的重点不再是“能不能抓到求是原文”，而是“线上环境里为什么被全部过滤掉或没保存进去”。
- 下一步优先排查三条线上链路：一是线上数据库里是否已有标题/URL 级重复导致被提前排除；二是 `merge_and_dedupe()` 之后到 `new_articles` 之间是否被线上 existing_urls / existing_titles 全量吃掉；三是 `save_articles()` 是否因线上 Supabase/RLS/字段问题返回 0。

### 2026-03-31 晚间页面加载补充
- 用户反馈网页“没法正常打开，一直转圈，加载不出来”。本地预览未复现前端运行时错误，但为降低首屏因浏览器存储异常或空数据结果造成的假性“无限加载”，已做一轮最小加固。
- `src/lib/utils.ts` 已新增安全存储访问方法，避免 `localStorage` / `sessionStorage` 在受限环境下抛错影响首屏。
- `src/App.tsx` 已改为安全读写首页筛选状态与滚动位置，并把首页空数据场景从持续显示“加载文章中...”改为明确提示“暂无可显示的文章数据”，避免用户误以为页面一直卡住。
- 本地已重新通过 build / lint / tsc，并用本地预览页验证首页可正常打开；当前浏览器里仅见百度统计 gif 请求中断，不影响页面主功能。

### 2026-04-01 继续排查自动搜索未新增问题
- 进一步对线上 `search_logs` 做查询后发现：用户 23:26 手动触发的那条 GitHub Actions 记录，确实是 `direct+kimi+baidu`；而 23:25 的两条旧记录分别来自浏览器端历史自动调度器，`api_used` 显示为 `deepseek` 与 `kimi+baidu`，并不是 GitHub Actions 重复执行，也不是时间记录错误。
- 这解释了为什么用户会看到“23:25 有两条、23:26 又有一条”：当时系统里同时存在“前端页面打开时本地自动搜索”与“GitHub Actions 后端自动搜索”两套链路，日志都写进了同一张 `search_logs` 表。
- 已在 `src/main.tsx` 停止初始化前端 `autoSearchScheduler`，后续只保留 GitHub Actions 作为正式自动搜索入口，避免再出现同一时间段多条来源不同但外观相似的搜索记录。

### 2026-04-01 继续排查求是《树立和践行正确政绩观》未入库原因
- 通过把本地最新 `ai_search.py` 直接接到线上数据库现有标题集复算，已确认问题不在“抓不到求是原文”，而在“标题去重阶段误杀”。
- 旧版 `is_title_duplicate()` 除了精确匹配外，还做了“子串包含”和“字符重合度 > 0.8”判断，这会把《树立和践行正确政绩观》误判为与库内旧文章《坚持把树立和践行正确政绩观摆在突出位置》重复。
- 误判发生后，求是原文链接、新华同文链接、人民网讲话库同文链接都会在标题去重阶段被提前丢弃，于是线上最后表现成“爬取 24、搜索 0、新增 0”。
- 已将标题去重收紧为“标题归一化后精确匹配”，保留对《求是》发布消息包装标题的归一化能力，但不再把“仅包含相同短语、并非同一篇”的文章误判为重复。
- 重新用线上可读到的现有标题集模拟后，当前至少已能稳定保留求是原文《树立和践行正确政绩观》；若再结合用户说明的其他库内已有人民网原文，最终结果就能回到用户预期的“只剩 1 篇求是原文”。

### 2026-04-01 自动搜索记录解释增强
- 已在 `.github/scripts/ai_search.py` 中补充自动搜索诊断信息，搜索完成后会把以下过程写入 `search_logs.details`：
  - 各来源命中数
  - 原始候选总数
  - 去重保留清单
  - 非原文/评论过滤清单
  - 库内标题重复清单
  - 本轮标题/链接重复清单
  - 库内已有 URL 过滤清单
  - 最终新增待审核清单
  - 保存结果与失败原因
- 已同步增强后台 [AdminPendingTab.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/admin/AdminPendingTab.tsx) 的执行记录展示，后续不再只是显示 24/0/0/0，而能直接看到每一步筛掉了什么、最后保留了什么。

### 2026-04-01 第二轮线上结果收口
- 用户复测时看到“去重保留 / 最终新增待审核”里仍出现 4 条：求是原文《树立和践行正确政绩观》、求是评论《以正确政绩观推动高质量发展》、以及人民网 `40692140`、`40692134` 两篇库内已有文章。
- 这说明上一轮修复已经解决了“求是政绩观原文被误杀”的问题，但还残留两个细节：
  - 求是评论文章仍可能因为正文里引用或拼接了总书记原文而误命中“作者：习近平”判断；
  - 库内已有链接/标题读取仍存在样本不全问题，导致部分已存在文章没被及时排除。
- 现已把求是原文判定进一步收紧为“标题附近正文抬头必须出现总书记署名特征”，不再只靠整页文本是否包含“作者：习近平”，从而过滤《以正确政绩观推动高质量发展》这类非总书记原文。
- 现已把 `get_existing_urls()` / `get_existing_titles()` 改为分页全量读取 `pending_articles + articles`，修复原来只拉前 2000 条导致 `https://jhsjk.people.cn/article/40692140` 与 `https://jhsjk.people.cn/article/40692134` 漏判为新增的问题。
- 用当前脚本接线上现有库再次模拟后，如果保留已经入待审核的求是原文这 1 篇，其余结果都会因“非原文”或“库内已存在”被正确排除；也就是说，回到用户最初那次运行时的库状态，最终新增待审核应只剩求是原文 1 篇。

### 2026-04-01 后台展示补充
- 已把后台搜索记录中的“筛选说明”区域改为默认折叠，用户需要时再展开查看明细，避免后台页面里单条执行记录占位过大。
- 已把“自动定时搜索”说明文案更新为当前真实链路：GitHub Actions 每日 8:00 / 20:00 定时运行，直抓人民网讲话数据库、新华社/新华网、求是网，并用 Kimi 联网补漏，统一去重后再与文章库比对。

### 2026-04-01 第三轮重复范围修正
- 用户继续反馈：前面几条待审核是“忽略掉了没入库”，因此不应把这些历史待审核记录算作“正式文章库重复”；当前预期应恢复为“只新增求是原文《树立和践行正确政绩观》”。
- 据此进一步修正了自动搜索的“已有内容”判定范围：
  - `articles` 仍作为正式文章库全量参与去重；
  - `pending_articles` 仅把 `status = pending` 的待处理记录视为重复；
  - 已忽略/已驳回的 `rejected` 记录不再阻断重新进入待审核。
- 同时补上了 URL 归一化中的协议统一，旧库里形如 `http://jhsjk.people.cn/article/40692140?isindex=1` 的链接，现在会与新抓到的 `https://jhsjk.people.cn/article/40692140` 正确视为同一篇。
- 还进一步修正了求是原文识别：既能识别正文真实标题是《树立和践行正确政绩观》、但页面标题包装成“《求是》杂志发表习近平总书记重要文章《树立和践行正确政绩观》”的情况，又不会把《以正确政绩观推动高质量发展》这类评论文章误当作原文。
- 以当前线上可读取的文章库重新模拟后，最终 `new_articles` 已回到 1 篇，正是：`https://www.qstheory.cn/20260331/904c5f95a8424cd0a5fbfb3f58a67d56/c.html`。

### 2026-04-01 顶部完成提示纠正
- 用户最新反馈中，自动搜索实际已经新增了 1 篇待审核，但后台顶部状态框仍显示“后台搜索完成！暂无新文章（可能已存在或工作流未找到）”。
- 已定位原因在前端 [githubActionsTrigger.ts](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/services/githubActionsTrigger.ts)：此前对 Supabase `head: true` 查询错误地读取了 `data.length`，而这类查询应读取返回的 `count` 字段，导致前后待审核数量对比恒为 0。
- 现已将轮询完成后的提示逻辑改为：
  - 优先读取最新 `search_logs` 的 `new_count`
  - 再参考 `details.final_new_articles_count`
  - 再参考 `details.save_result.saved_count`
  - 最后才使用 `pending_articles` 的真实 `count` 差值兜底
- 同时已把 [AdminDashboard.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/AdminDashboard.tsx) 的顶部提示文案改为直接复用该结果，确保“顶部状态框”和“执行记录里实际新增数量”一致。

### 2026-04-01 定时工作流复核
- 已再次复核 [ai-auto-search.yml](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/.github/workflows/ai-auto-search.yml#L3-L8)：
  - `0 0 * * *` 对应北京时间每日 8:00
  - `0 12 * * *` 对应北京时间每日 20:00
- 当前自动搜索正式入口仍只有 GitHub Actions；前端旧自动调度器已停用，因此不会再出现“本地页面打开也写搜索日志”的干扰。
- 在当前脚本下，来源直抓、求是原文判定、URL/标题去重、库内重复范围、日志解释和顶部完成提示已对齐，按现有规则应能在早晚 8 点稳定做到“不重不漏地保留符合条件的新文章”。

### 2026-04-01 手动触发与定时任务区分
- 用户继续反馈：虽然手动点击搜索与定时搜索共用同一个 GitHub Actions 工作流，但后台执行记录里手动触发的记录也显示成了“定时任务”，会影响判断当天早晚 8 点是否真的自动跑过。
- 已定位原因在 [ai_search.py](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/.github/scripts/ai_search.py)：写 `search_logs` 时把 `details.search_type` 固定写成了 `auto`，没有区分 `schedule` 与 `workflow_dispatch` 两种 GitHub Actions 事件来源。
- 现已新增 `get_search_trigger_metadata()`，会从 `GITHUB_EVENT_NAME` 与 `GITHUB_EVENT_PATH` 中读取真实触发方式：
  - `schedule` 记为 `search_type = auto`，后台展示“定时任务”
  - `workflow_dispatch` 记为 `search_type = manual`，后台展示“手动搜索”
- 同时日志里还会额外保存：
  - `triggered_from`
  - `github_event_name`
  - `trigger_label`
  这样后续即使再排查异常记录，也能明确知道这条到底是系统定时触发还是你手动点按钮触发的。

### 2026-04-01 首页计数与摘要一致性优化
- 用户继续提出两个体验问题：一是首页显示的总篇数希望包含政绩观专题文章；二是 AI 识别 URL 时看到的摘要、待审核框里的摘要与最终详情页摘要长度和内容不一致，影响审核判断。
- 已确认首页总数此前只基于 `getArticles()` 返回的普通文章列表统计，而 `getArticles()` 会主动排除 `isZhengjiguan = true` 的专题文章，因此总数确实未包含政绩观专题。
- 现已在 [App.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/App.tsx) 中单独拉取 `getZhengjiguanArticles()`，并把首页统计总数改为“普通文章数 + 政绩观专题文章数”，从而与后端文章总量口径更一致。
- 同时对摘要链路做了统一：
  - [utils.ts](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/lib/utils.ts) 里的 `normalizeSummaryText()` 默认策略已从偏短改为“120-220 字、2-3 句、尽量保留原话”；
  - [aiSummaryService.ts](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/services/aiSummaryService.ts) 的提示词和兜底裁剪规则同步放宽，避免详情页摘要过短；
  - [useAdminArticleManagement.ts](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/hooks/useAdminArticleManagement.ts) 现在会在 AI URL 识别、手动粘贴识别、待审核转正式文章时统一归一化摘要；
  - [pendingArticleService.ts](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/services/pendingArticleService.ts) 加载待审核文章时也统一做同样的摘要归一化。
- 这样现在审核框里看到的摘要、保存到详情页里的摘要和最终详情页展示的摘要会尽量保持一致，不再出现“审核时很长、发布后突然变很短”的落差。
- 另已在 [AdminAddArticleDialog.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/admin/AdminAddArticleDialog.tsx) 与 [AdminPendingTab.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/admin/AdminPendingTab.tsx) 中补充摘要字数展示，方便审核时判断摘要是否过长或过短。
- 关于“原文提取是否还有必要保留”：当前仍建议保留。它不需要再额外多发一次识别请求，主要成本只是同一次模型输出里多带了一段正文；保留后对审核核对、后续再次生成摘要/解读、导出文档都有价值。若后续长期确认完全不用，再单独评估关闭以节省少量 token 输出成本。

### 2026-04-02 手机端详情页语音播报修复
- 用户反馈手机端每篇文章详情页的语音播报经常直接提示“音频播放失败请重试”，要求优先修复该功能。
- 已确认问题主要集中在 [DetailPage.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/DetailPage.tsx) 的语音播报链路：之前的实现会在部分手机浏览器里较早回退到外部音频方案，而移动端对长文本、外部音频地址和自动播放限制都更敏感，因此容易出现开始即失败。
- 现已在详情页里补充更稳的原生播报策略：
  - 优先使用 `speechSynthesis` 原生语音；
  - 将整段播报文本按句子切分为更短的分段队列，逐段播放；
  - 增加播报启动超时控制，如果原生语音在短时间内没有真正开始，再自动切回备用方案；
  - 统一停止播放逻辑，避免重复点击后出现原生播报和备用音频状态相互打架。
- 同时已把播报文本长度收敛为“标题 + 摘要 + 截断后的正文/解读”，减少手机端一次性播报过长文本时更容易静默失败、中断或卡住的问题。
- 语音播报设置弹窗里的提示文案也已同步改为更中性的说明，不再把某类浏览器固定写死成“只能走在线语音服务”。
- 本轮已重新通过 build / lint / tsc 与 diagnostics，当前详情页代码未引入新的编译或类型错误。

### 2026-04-02 华为安卓机型定向兼容加固
- 用户在首次上线后继续反馈：自己的安卓华为手机上，详情页语音播报依然直接提示“音频播放失败请重试”，说明上一轮修复后，华为安卓环境仍然被错误带到了外部音频回退链路。
- 结合现有实现再次排查后，进一步确认风险点主要有三类：
  - 安卓/华为浏览器原生 `speechSynthesis` 启动比桌面端更慢，之前 1.5 秒的等待窗口过短，容易“其实即将开始播报，却被提前判定为失败”；
  - 分段播放时每段前都再次执行 `cancel()`，在部分安卓浏览器里会把刚准备开始的新一段播报直接打断；
  - 华为/安卓系统语音引擎对自定义 `voice` 兼容性不稳定，强行绑定某个中文 voice 反而可能让原生播报根本起不来。
- 因此这次已继续在 [DetailPage.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/DetailPage.tsx) 做定向兼容处理：
  - 新增安卓/华为环境识别；
  - 在安卓/华为环境下默认不强行绑定自定义中文 `voice`，优先只指定 `lang = zh-CN`；
  - 把原生播报启动等待窗口放宽，并在判定失败前先检查 `speechSynthesis.speaking` / `speechSynthesis.pending` 状态；
  - 去掉每段播报前重复 `cancel()` 的做法，避免安卓浏览器在分段切换时误打断新队列；
  - 若首次原生启动仍未成功，则优先以更短分段自动再试一次，而不是立刻回退到外部音频方案。
- 当前这轮修复的目标已经从“兼容手机端”进一步收紧到“优先保证华为安卓详情页先把原生语音成功拉起来”，尽量不再命中失败概率更高的外部音频回退。
- 本轮已再次通过 build / lint / tsc 与 diagnostics，当前代码未引入新的编译、类型或编辑器诊断错误。

### 2026-04-02 全量手机端与微信环境语音兼容继续加固
- 用户继续明确要求：不仅华为安卓要可用，苹果手机和各种手机微信内置浏览器也要尽量适配；同时要求把最新修复版本推上去再继续测试。
- 基于这一轮要求，详情页语音播报策略继续从“华为安卓定向修复”扩展为“所有手机端 + 微信环境优先稳定原生语音”：
  - 新增 iOS 与微信环境识别；
  - 将“避免外部音频回退”的策略扩展到所有手机端与微信内置浏览器，尽量不再过早走外部 TTS 音频 URL；
  - 移动端与微信环境下继续缩短单段播报长度，并把总播报内容进一步收敛为“标题 + 摘要 + 前段正文”，优先保证先稳定播出来；
  - 对原生语音增加 keep-alive 机制，播报过程中定时执行 `speechSynthesis.resume()`，降低 iPhone / 微信环境中被系统暂停或挂起后中断的概率；
  - 原生启动失败时从“一次重试”扩展为“多轮更短分段重试”，尽量先把原生语音拉起来，再考虑是否回退。
- 这次调整的核心思路是不再把“所有移动端问题”都归结到备用音频方案，而是承认手机端真正更稳的往往仍然是浏览器原生 `speechSynthesis`，因此优先提高原生链路的启动成功率和持续播报稳定性。
- 同步地，语音播报弹窗底部说明文案也已改为移动端口径：提示“手机端与微信环境会优先尝试浏览器内置语音，先播放标题和摘要，再继续正文”，避免界面文案与实际策略不一致。
- 本轮已再次通过 build / lint / tsc 与 diagnostics，当前代码未引入新的编译、类型或编辑器诊断错误。

### 2026-04-02 同源服务端语音方案落地
- 用户再次复测后仍反馈“不行”，并明确提出：如果微信内部打开网页本身就不支持，那就不要再只赌浏览器兼容，应该考虑“内置语音包”或其他方式，核心目标是确保语音播报功能真正正常。
- 这一轮因此不再继续只围绕前端 `speechSynthesis` 做兼容，而是直接落地更可控的同源服务端语音方案：
  - 新增 [api/tts.js](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/api/tts.js)，作为部署在站点同源域名下的语音代理接口；
  - 接口会在服务端请求中文 TTS 音频流，再由前端以 `/api/tts?text=...` 的同源地址顺序播放；
  - 对手机浏览器和微信内置浏览器来说，这比“前端直接访问第三方外链音频”稳定得多，也避免了跨域、外链音频被拦、Referer 校验或浏览器策略差异带来的失败。
- 与此同时，前端详情页也同步改为分场景策略：
  - 手机端 / 微信环境默认优先走同源 `/api/tts` 音频播放，相当于站点自己提供稳定语音包出口；
  - 桌面端仍保留原生 `speechSynthesis`，避免影响原本已可正常播报的浏览器体验；
  - 移动端音频分段长度继续控制在更短范围内，减少单个音频片段过长导致加载或播放失败的问题。
- 为保证部署后 `/api/tts` 不会被原有 SPA 路由重写吞掉，[vercel.json](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/vercel.json) 也已同步补充 `/api/*` 显式保留规则。
- 这一轮的核心变化可以理解为：之前是“尽量让浏览器自己说话”，现在则改成“站点自己生成音频给浏览器播”，这对微信场景尤其关键。
- 本轮已再次通过 build / lint / tsc 与 diagnostics，当前代码未引入新的编译、类型或编辑器诊断错误。

### 2026-04-02 GitHub Pages 静态部署兼容修正
- 用户随后明确说明：站点实际并不是部署在 Vercel，而是 GitHub Pages；当前线上依旧提示“调用语音包失败”。
- 这说明上一轮新增的 `/api/tts` 同源接口虽然思路正确，但与“Pages 纯静态部署”本身冲突：GitHub Pages 只能托管静态文件，不能执行本地服务端接口，因此手机端/微信一旦命中 `/api/tts`，实际上就会直接失败。
- 基于这个事实，本轮改成了 Pages 兼容逻辑：
  - 在 [DetailPage.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/DetailPage.tsx) 中新增 `github.io` 运行环境识别；
  - 如果当前运行在 Pages 下，则不再请求本地 `/api/tts`；
  - 改为为每个语音片段准备多组可直接播放的外部 TTS 音频地址，按顺序自动重试；
  - 某一个语音源失败时，前端会自动切换到下一个，而不是整段立即报错终止。
- 这次修正的核心目标不是“让 Pages 具备服务端能力”，而是承认 Pages 没有服务端这一前提，并把前端语音方案重新改回“纯静态站也能尝试工作”的模式。
- 当前 `api/tts.js` 仍保留在仓库中，后续若站点迁移到 Vercel / Cloudflare / Netlify 等支持函数的平台，可以继续启用同源语音接口；但在 GitHub Pages 环境下，实际生效的是“多外部语音源顺序回退”版本。
- 本轮已再次通过 build / lint / tsc 与 diagnostics，当前代码未引入新的编译、类型或编辑器诊断错误。

### 2026-04-02 Pages 自定义域名下的语音误判继续修正
- 在继续复核后，又发现上一版 Pages 兼容逻辑仍有一个隐藏问题：它把“是否运行在 Pages”近似写成了“当前主机名是否以 `github.io` 结尾”。
- 这个判断只适用于默认的 `*.github.io` 域名；如果 GitHub Pages 绑定了自定义域名，前端就会误以为自己不在 Pages，于是又把不存在的 `/api/tts` 放回候选列表，用户侧仍可能继续看到“调用语音包失败”。
- 为了彻底切断这个误判，本轮把 [DetailPage.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/DetailPage.tsx) 的判断方式改成：
  - 仅在本地开发环境下启用同源 `/api/tts`；
  - 或者后续若迁移到支持函数的平台，再通过显式环境变量 `VITE_ENABLE_TTS_PROXY=true` 手动打开；
  - 其余线上静态部署默认一律不依赖本地 `/api/tts`，避免再次因为部署平台判断失误而访问不存在的接口。
- 同时也把详情页播报入口调整为更稳的顺序：
  - 只要浏览器支持 `speechSynthesis`，就先尝试原生播报；
  - 若原生多轮启动仍失败，再回退到多个外部音频源顺序重试；
  - 外部音频的首个 `play()` 若失败，也会继续切换下一个音频源，而不是第一下失败就立刻结束。
- 这次修正的重点不是新增新的语音渠道，而是把“Pages + 自定义域名”这一之前漏掉的线上条件补齐，确保 GitHub Pages 真正不再触发本地 `/api/tts` 依赖。

### 2026-04-02 浏览器端中文离线语音包接入
- 用户进一步明确诉求：既然华为手机微信里继续报“当前浏览器语音播放失败”，那就不要再只依赖浏览器自带能力或外部音频链接，而是尽量“下载一个语音包放进去”，把文章/新闻类内容的播报链路做得更稳。
- 在 GitHub Pages 这种纯静态部署下，真正可落地的方式不是服务端合成，而是把中文语音模型放到浏览器端执行：
  - 本轮新增了 `@mintplex-labs/piper-tts-web` 与 `onnxruntime-web` 依赖；
  - 前端现在可以在用户浏览器内下载并缓存中文 `zh_CN-huayan-x_low` 语音模型；
  - 模型首次下载约 20MB，下载完成后会保存在当前浏览器本地，下次进入详情页时可直接复用。
- [DetailPage.tsx](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/src/components/DetailPage.tsx) 已同步调整为新的优先级：
  - 华为设备或微信环境下，点“开始播放”会优先尝试浏览器端离线语音包；
  - 其余环境仍保留原生 `speechSynthesis` 优先；
  - 如果离线语音包初始化或播放失败，最后仍会回退到现有外部音频候选源，避免出现单一路径失败后整个功能不可用。
- 为了让这条链路对用户可见、可控，语音播报弹窗中也新增了：
  - “下载语音包”按钮；
  - 当前是否已就绪的状态标记；
  - 下载/初始化进度条与实时状态文案。
- 这次改动的核心意义是：在 GitHub Pages 静态站里，终于补上了一条不依赖本地 `/api/tts`、也不完全依赖微信/华为浏览器原生 TTS 的第三条播报路径，更贴近用户所说的“把语音包装进去”。

### 2026-04-02 规则记忆补充
- 用户补充了新的长期协作要求：每次正式运行前，不仅要读规则文件，还要一并通读 `todolist.md`、`项目框架.md`、`项目迭代记录.md`，先恢复项目记忆，再开始动手。
- 这条要求已写入 [.trae/rules/project_rules.md](file:///e:/ccswitch/AI编程工作目录/讲话网站/app/.trae/rules/project_rules.md)，后续将按“四文件预读”的固定流程执行。
- 这样做的价值主要是减少反复检索代码和重复排查历史上下文；虽然每轮都会多读几份短文档，但整体上通常比再次大范围搜索代码、重复试错更省上下文和 token。
