# 项目迭代记录

## 2026-04-20 本地真人测试 + 顶部分页与快速跳页收尾

### 本次目标
- 本地真人模拟测试首页分页回顶是否稳定
- 在分页体验上继续做下一步优化，减少长列表场景的重复滚动
- 确认无误后推送部署，并把结果同步回 rules 文档

### 实际改动
- 启动本地 Vite dev server，对首页分页进行真人模拟测试
- 在 `App.tsx` 首页列表上方补充顶部分页栏，支持首页、上一页、页码、下一页、末页
- 在顶部工具区新增快速跳页输入框与跳转按钮，便于长列表直达目标页
- 保留底部分页区，形成“顶部快速控制 + 底部翻页收尾”的双入口结构
- 本地实测把每页切到 20 后，验证首页、末页、页码按钮、快速跳页都能切换到目标页，并回到列表顶部
- 将本轮首页体验优化、验证结果和长期结构说明同步回 `.trae/rules` 文档

### 当前状态
- ✅ 本地真人模拟测试通过
- ✅ 首页 / 末页 / 页码 / 上一页 / 下一页回顶正常
- ✅ 快速跳页可正常切换到目标页
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ 已推送部署：`e30a5fa`、`d9b7e1e`

### 提交记录
- `e30a5fa` feat: 增强部署可观测性与分页回顶稳定性
- `d9b7e1e` feat: 增加顶部分页与快速跳页

### 遗留事项
- 继续观察线上 GitHub Pages 缓存更新是否及时，重点确认页脚版本号与最新 commit 一致
- 后续可继续优化首页空结果态、筛选反馈与更大页数下的长列表导航体验

## 2026-04-18 最高优先优化收尾（部署版本可视化 + 人民网讲话库时区修复 + 分页回顶继续修复）

### 本次目标
- 增加前台部署版本可视化，便于判断线上页面是否为最新包
- 修复 `search_people_jhsjk()` 中 UTC/北京时间混用导致的日期判断偏差
- 继续修复首页/末页点击后仍未稳定回到顶部的问题

### 实际改动
- vite.config.ts 构建阶段注入 `__APP_VERSION__`、`__APP_COMMIT_HASH__`、`__APP_BUILD_TIME__`
- Footer.tsx 新增部署信息展示，前台可直接看到版本号、commit 短哈希和构建时间
- env.d.ts 补充构建常量声明，保持 TypeScript 类型完整
- ai_search.py 抽出 `get_beijing_now()` 与 `get_recent_valid_dates()`，统一人民网讲话库搜索中的北京时间基准
- `search_people_jhsjk()` 改为复用统一日期函数，并修正无日期时的兜底值，避免 UTC 与北京时间跨日导致误判
- App.tsx 新增 `pendingPageTargetRef`，将分页回顶和目标页绑定
- `scrollToListTop()` 改为优先 `scrollIntoView()` 再做一次顶部对齐，增强首页/末页大跨度翻页的稳定性
- 分页回顶 effect 改为 `useLayoutEffect`，确保在目标页渲染并完成布局后再执行回顶

### 当前状态
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ py_compile 通过
- ✅ 前台已可直接识别当前部署版本
- ✅ 分页回顶逻辑已进一步收紧到目标页切换完成后执行

### 提交记录
- `e30a5fa` feat: 增强部署可观测性与分页回顶稳定性

### 遗留事项
- 仍需在线上极端场景验证：首页滚到最底后点击首页/末页，是否 100% 回到列表顶部
- jhsjk.people.cn 仍存在动态渲染抓取不完整的长期问题，需后续继续补强

## 2026-04-18 首页分页回顶修复（首页/末页）

### 本次目标
- 修复点击首页/末页后未稳定回到列表顶部的问题
- 保持上一页、下一页、页码直选与首页/末页的回顶行为一致

### 实际改动
- App.tsx 中新增待执行回顶标记 `pendingListScrollBehaviorRef`，避免在 `setCurrentPage()` 之前过早滚动
- `handlePageChange()` 改为先记录待执行回顶，再更新页码状态
- 新增基于 `currentPage`、`pageSize` 的统一回顶 effect，等待分页状态生效并完成一轮渲染后再滚动到列表顶部
- 保留现有 `scrollToListTop()` 与滚动恢复逻辑，缩小改动面，避免影响详情返回首页时的滚动恢复体验

### 当前状态
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ 首页/上一页/页码/下一页/末页统一走分页后回顶逻辑

### 提交记录
- `9d82ce8` fix: 修复首页末页分页回顶时机

### 遗留事项
- 仍需用户在线上再验证一次极端场景：从页面最底部点击首页/末页是否稳定回顶

## 2026-04-18 首页分页交互继续优化（首页/末页 + 移动端紧凑）

### 本次目标
- 为首页分页补充首页/末页跳转
- 优化移动端页码区，避免按钮过宽、换行拥挤

### 实际改动
- 调整 `getVisiblePages()`，将可见页码数量收紧到当前页附近，减少移动端一屏内按钮数量
- 分页栏新增“首页”“末页”按钮，保留上一页/下一页
- 分页按钮尺寸在移动端改为更紧凑的 `text-xs + px-2.5`，在较大屏幕恢复 `text-sm + px-3`
- 当页码与首尾存在间隔时，继续使用省略号提示隐藏页码

### 当前状态
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ 支持首页/末页快捷跳转
- ✅ 移动端页码区更紧凑，换行压力更小

### 提交记录
- 已并入后续分页优化提交

### 遗留事项
- 仍建议在线上真机再看一次极窄屏下分页区换行效果

## 2026-04-18 首页分页交互优化（页码直选 + 回顶）

### 本次目标
- 底部分页不只支持上一页/下一页，还要支持直接选择页码
- 用户点击页码或上一页/下一页后，页面应自动回到对应列表顶部

### 实际改动
- 在 App.tsx 新增 `getVisiblePages()`，根据总页数和当前页动态生成可点击页码，页数较多时自动显示省略号
- 新增 `listTopRef` 与 `scrollToListTop()`，翻页与点击页码后统一滚动到列表顶部
- 抽出 `handlePageChange()` 统一处理页码跳转、边界保护和回顶行为
- 首页底部分页栏改为“上一页 + 页码按钮 + 下一页”的组合，并保留当前页/总页数文案

### 当前状态
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ 支持页码直选
- ✅ 点击页码、上一页、下一页后自动回到列表顶部

### 提交记录
- 已并入后续分页优化提交

### 遗留事项
- 仍需线上真人验证移动端窄屏下页码按钮换行后的触控体验

## 2026-04-18 首页分页改造（每页可选）

### 本次目标
- 首页列表增加分页能力，默认每页 50 条
- 支持用户切换每页条数：20 / 50 / 100 / 500
- 保持现有筛选、搜索、排序链路不变

### 实际改动
- 在 App.tsx 新增分页状态：`currentPage`、`pageSize`，并持久化到 sessionStorage
- 新增 `PAGE_SIZE_OPTIONS = [20, 50, 100, 500]`，默认每页 50
- 保留既有筛选与搜索逻辑，基于 `filteredSpeeches` 增加分页切片 `pagedSpeeches`
- 新增页码边界保护：筛选条件变化自动回到第 1 页，结果总页数变化时自动纠正当前页
- 首页主列表区域新增分页信息区、每页条数下拉选择、上一页/下一页翻页按钮
- `ContentList` 改为接收分页后的 `pagedSpeeches`，其余渲染逻辑不变

### 当前状态
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ 默认每页 50，支持 20/50/100/500 切换
- ✅ 筛选条件变化后自动回到第一页

### 提交记录
- 已并入后续分页优化提交

### 遗留事项
- 仍需线上手动验证分页切换与滚动恢复体验在移动端表现

## 2026-04-18 评论过滤与历史去重修复

### 本次目标
- 修复新华社、人民日报评论/回响类非原文被误抓入待审核的问题
- 修复求是原文与已入库文章未命中历史去重的问题
- 修复待审核库与正式文章库未做联合标题去重的问题

### 实际改动
- ai_search.py 新增评论/非原文标题关键词与正则过滤，拦截“总书记的关切·落地的回响”、评论、述评、观察、解读、综述、侧记等标题
- search_xinhua_mrdx() 与 search_rmrb() 在抓取阶段增加非原文标题过滤，避免评论类文章进入 merge 流程
- search_qstheory() 收紧原文入口判定，不再把“《求是》杂志发表习近平总书记重要文章《xxx》”这类报道标题当作原文收录
- 新增 normalize_article_title()、titles_look_duplicate() 与 get_existing_articles()，改为同时读取 pending_articles 和 articles 的 URL、标题进行历史去重
- 历史标题去重改为使用标题归一化 + 包含关系匹配，兼容求是标题前缀差异与库内已有致电标题
- merge_and_dedupe() 内部标题简化逻辑统一复用 simplify_title()，保持本轮去重与历史去重规则一致

### 当前状态
- ✅ py_compile 通过
- ✅ npm run lint 通过
- ✅ 评论/回响类标题已在抓取阶段过滤
- ✅ 历史标题去重已覆盖 pending_articles + articles 双表

### 提交记录
- 已并入后续搜索修复提交

### 遗留事项
- 仍需在线上工作流中观察评论类标题过滤是否还有漏网变体
- search_people_jhsjk() 时区问题仍待后续修复
