# 项目迭代记录

## 2026-04-20 AI 搜索误抓修复（非总书记直接相关过滤）

### 本次目标
- 修复 AI 搜索链路中“非总书记直接相关”标题被误抓的问题
- 覆盖用户给出的三类样例：领会解读类、主席特别代表/特使出席类
- 保持既有评论类过滤、历史去重、来源校验能力不退化

### 实际改动
- 在 `.github/scripts/ai_search.py` 新增 `NON_DIRECT_XI_KEYWORDS` 与 `NON_DIRECT_XI_PATTERNS`
- 新增 `is_non_direct_xi_title(title)`，用于识别并过滤非总书记直接相关标题，重点规则包括：
  - “领会总书记/学习贯彻总书记/贯彻落实总书记”类解读标题
  - “习近平主席特使/特别代表/受习近平主席委派（指派）”类代行出席标题
- 在 `merge_and_dedupe()` 的统一入库口 `add()` 中加入该过滤，确保不论来源（新华社/人民日报/百度/人民网兜底）都统一生效
- 增加日志字段：`non_direct_rejected_count` 与 `non_direct_rejected`，方便后续在 search log 中定位被该规则淘汰的条目
- 保留并兼容现有 `is_non_original_title()` 评论/解读过滤逻辑，形成“评论类 + 非直接相关”双层过滤

### 当前状态
- ✅ 误抓过滤逻辑已完成
- ✅ 用户给出的三条样例已覆盖到过滤规则
- ✅ 典型正样本“习近平会见法国总统马克龙”验证可保留
- ✅ `python -m py_compile .github/scripts/ai_search.py` 通过
- ✅ `npm.cmd run lint` 通过
- ✅ `npm.cmd run build` 通过
- ⏳ 待提交并推送部署

### 提交记录
- 待本轮提交

### 遗留事项
- 需在线上观察是否还有新的“非总书记直接相关”变体漏网，若有再补充关键词与模式
- 继续跟踪 jhsjk.people.cn 动态渲染抓取完整性问题（长期待办）

## 2026-04-20 首页空结果态与筛选反馈优化

### 本次目标
- 在现有分页基础上继续优化首页筛选体验
- 当搜索词与筛选条件组合后无结果时，给出更明确的反馈与恢复入口
- 确认无误后继续验证并准备推送部署

### 实际改动
- 在 `App.tsx` 首页列表区域新增空结果态分支，仅在文章数据已加载但筛选结果为空时显示
- 空结果态增加视觉图标、说明文案和当前筛选摘要，帮助用户快速判断为什么没有结果
- 新增“恢复全部筛选”操作，同时按场景显示“仅清空搜索词”或“仅清空筛选条件”按钮，减少重复手动点击成本
- 空结果态复用现有首页搜索与筛选状态，不引入额外依赖，也不改变原有分页和滚动恢复主链路
- 同步回写 `.trae/rules` 文档，保持这轮首页体验优化与文档一致

### 当前状态
- ✅ 空结果态 UI 与交互已完成
- ✅ 当前筛选摘要与恢复操作已接入首页状态
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ⏳ 待本地补测空结果态交互后推送部署

### 提交记录
- 待本轮验证通过后提交

### 遗留事项
- 推送后仍需继续观察线上 GitHub Pages 缓存刷新与移动端空结果态展示效果

## 2026-04-20 本地真人测试 + 顶部分页与快速跳页收尾

### 本次目标
- 本地真人模拟测试首页分页回顶是否稳定
- 在分页体验上继续做下一步优化，减少长列表场景的重复滚动
- 确认无误后推送部署，并把结果同步回 rules 文档

### 实际改动
- 启动本地 Vite dev server，对首页分页进行真人模拟测试
- 在 `App.tsx` 首页列表上方补充顶部分页栏，支持首页、上一页、页码、下一页、末页
- 在顶部工具区新增快速跳页输入框与跳转按钮，便于长列表直达目标页
- 保留底部分页区，形成“顶部快速控制 + 底部翻页收尾”的双入口结构
- 本地实测把每页切到 20 后，验证首页、末页、页码按钮、快速跳页都能切换到目标页，并回到列表顶部
- 将本轮首页体验优化、验证结果和长期结构说明同步回 `.trae/rules` 文档

### 当前状态
- ✅ 本地真人模拟测试通过
- ✅ 首页 / 末页 / 页码 / 上一页 / 下一页回顶正常
- ✅ 快速跳页可正常切换到目标页
- ✅ npm run lint 通过
- ✅ npm run build 通过
- ✅ 已推送部署：`e30a5fa`、`d9b7e1e`

### 提交记录
- `e30a5fa` feat: 增强部署可观测性与分页回顶稳定性
- `d9b7e1e` feat: 增加顶部分页与快速跳页

### 遗留事项
- 继续观察线上 GitHub Pages 缓存更新是否及时，重点确认页脚版本号与最新 commit 一致
- 后续可继续优化首页空结果态、筛选反馈与更大页数下的长列表导航体验
